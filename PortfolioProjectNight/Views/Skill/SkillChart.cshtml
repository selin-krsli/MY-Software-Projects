@model List<PortfolioProjectNight.Models.Skill>
@{
    ViewBag.Title = "SkillChart";
    Layout = "~/Views/Admin/Index.cshtml";
}
<div class="alert alert-info"><strong>Yetenek Dereceleri</strong></div>

<!DOCTYPE html>
<html>
<script src="https://www.gstatic.com/charts/loader.js"></script>

<body>
    <div id="myChart" style="width:100%; max-width:1000px; height:700px;"></div>

    <script>
google.charts.load('current', {'packages':['corechart']});
google.charts.setOnLoadCallback(drawChart);

function drawChart() {

// Set Data
const data = google.visualization.arrayToDataTable([
  ['Skilll', 'Rate'],
@foreach (var item in Model)
{
    @: ['@item.SkillName', @item.Rate],
}
]);

// Set Options
const options = {
    title: 'Yetenek Dereceleri',
    hAxis: { title: 'Yetenek' },
    vAxis: { title: 'Dereceleri' },
    legend: { position: 'none' },
    colors: ['#5f9ea0']
};

// Draw
const chart = new google.visualization.BarChart(document.getElementById('myChart'));
chart.draw(data, options);

}
    </script>

</body>
</html>





